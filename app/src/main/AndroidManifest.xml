<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bankuishapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_github_ic"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_github_ic"
        android:supportsRtl="true"
        android:theme="@style/Theme.BankuishApp"
        android:usesCleartextTraffic="true" >
        <activity
            android:name="com.example.bankuishapp.activities.itemsearch.ItemActivity"
            android:exported="true" />
        <activity
            android:name="com.example.bankuishapp.activities.itemsearch.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.BankuishApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>